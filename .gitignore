# ============================================================
# 这个仓库是要发布到 GitHub Pages 的，所以只保留"页面运行时真正要读的文件"：
#   index.html  +  images/*.webp
# 其余一律留本地，不进仓库。
# ============================================================

# 原始素材（34MB）。塔罗牌的高清原图，只用于生成 images/ 里的 WebP。
# 它已经在历史提交里，需要时用：
#   git checkout <旧提交> -- 塔罗牌/
塔罗牌/

# 已被 WebP 取代的旧 PNG（27MB）。页面现在一张 png 都不引用。
# 同样可从历史里取回：
#   git checkout <旧提交> -- images/
images/*.png

# 验证与排版过程中产出的截图、临时页面、浏览器配置目录
preview/
_*.html

# 改版前的单文件备份
*.bak-*
